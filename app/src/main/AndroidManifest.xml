<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.felipe.buracometro_v5">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="com.example.felipe.buracometro_v4.permission.MAPS.RECEIVE" />

    <permission
        android:name="com.example.felipe.buracometro_v4.permission.MAPS.RECEIVE"
        android:protectionLevel="signature" >
    </permission>


    <application
        android:name=".util.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">

        <activity android:name=".activities.TelaSplash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".activities.TelaPrimeiroAcesso"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".activities.TelaLogin"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".activities.TelaCadastro"
            android:label="@string/app_name">
        </activity>

        <receiver
            android:name=".util.ConnectivityReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAbquc0dVzcVR2azJBrCIiP-beBspvVEgw"/>

    </application>

</manifest>